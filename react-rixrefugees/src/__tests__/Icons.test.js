import { render, fireEvent } from '../test-utils';

import CustomCheckCircleIcon from "../Components/utils/Icons/CustomCheckCircleIcon";
import CustomCheckIcon from "../Components/utils/Icons/CustomCheckIcon";
import CustomCloseIcon from "../Components/utils/Icons/CustomCloseIcon";
import CustomDeleteIcon from "../Components/utils/Icons/CustomDeleteIcon";
import CustomDescriptionIcon from "../Components/utils/Icons/CustomDescriptionIcon";
import CustomCHourglassIcon from "../Components/utils/Icons/CustomHourglassIcon";
import CustomScheduleIcon from "../Components/utils/Icons/CustomScheduleIcon";

describe('CustomCheckCircleIcon component', () => {
    test('it renders', () => {
      render(<CustomCheckCircleIcon />)
    });
})

describe('CustomCheckIcon component', () => {
    test('it renders', () => {
      render(<CustomCheckIcon />)
    });
})

describe('CustomCloseIcon component', () => {
    test('it renders', () => {
      render(<CustomCloseIcon />)
    });
})

describe('CustomDeleteIcon component', () => {
    test('it renders', () => {
      render(<CustomDeleteIcon />)
    });
})

describe('CustomDescriptionIcon component', () => {
    test('it renders', () => {
      render(<CustomDescriptionIcon />)
    });
})

describe('CustomCHourglassIcon component', () => {
    test('it renders', () => {
      render(<CustomCHourglassIcon />)
    });
})

describe('CustomScheduleIcon component', () => {
    test('it renders', () => {
      render(<CustomScheduleIcon />)
    });
})