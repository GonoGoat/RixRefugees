import React from "react";
import DeleteIcon from '@material-ui/icons/Delete';

const CustomCheckCircleIcon = () => (
    <DeleteIcon fontSize='inherit' style={{color : "black"}}/>
);

export default CustomCheckCircleIcon;